# Optional GPU dependencies - install these ONLY inside the AMD/ROCm Docker
# image (see Dockerfile.amd) or a CUDA box; the base requirements.txt
# intentionally has no torch dependency so the app installs fast and runs
# fully on CPU everywhere else (app/gpu/device_manager.py treats torch as
# optional and falls back to CPU cleanly if this file was never installed).
#
# AMD ROCm (matches docker-compose.amd.yml's rocm/pytorch base image):
#   pip install -r requirements-gpu.txt --index-url https://download.pytorch.org/whl/rocm6.1
# NVIDIA CUDA:
#   pip install -r requirements-gpu.txt --index-url https://download.pytorch.org/whl/cu121
torch==2.3.1
